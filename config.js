var config = {};

// notes
// If left blank then will not display
// Add a note which displays. For example you might want to put server locations, asstet tag, etc.
config.notes = "";

// roles
// If left blank then will not display
// Like a note but will be labeled as roles and can be filled with what the server's roles are, like Web Server or something
config.roles = "";

module.exports = config;